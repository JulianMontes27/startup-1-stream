Roadmap
Sesion 1: 
Pre: install shadcn (styles) DONE
1. Autenticacion (auth js) DONE
2. Prisma ORM con Mongo db (base de datos + ORM) DONE
3. Setup auth flow DONE
4. State management (Zustand) DONE

pre: fix the add-service modal

- if the user agrees to let the app use their location, use the geolocation, if not, they have to manually setup their location

6. Fix compliance and regulatory issues with geolocation
